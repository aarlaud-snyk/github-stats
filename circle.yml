version: 2
jobs:
  build:
    docker:
      - image: node:8.0
    steps:
      - run:
          name: "Build"
          command: |
            npm install
      - run:
          name: "Test"
          command: |
            npm test
      - run:
          name: "Monitor"
          command: |
            snyk monitor
